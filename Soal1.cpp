#include <stdio.h>

int main(){
    int n, a;
    printf("Masukan jumlah angka: ");
    scanf("%d", &n);

    for (int i=1; i<=n; i++){
        printf("%d", n);
        a +=n;
    }
    return 0;
}